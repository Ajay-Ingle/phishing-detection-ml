[pytest]
# Ensures `src` and `api` are importable as packages when running plain
# `pytest` from the repo root, without needing `python -m pytest` or a
# tests/__init__.py. Required for GitLab CI, which invokes pytest directly.
pythonpath = .
testpaths = tests
